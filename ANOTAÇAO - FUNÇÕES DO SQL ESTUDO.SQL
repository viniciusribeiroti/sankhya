SELECT DTENTRADA, 
	  LAST_DAY(TO_CHAR(DTENTRADA, 'DD/MM/yy')), MENSAGEM, ASSUNTO,
       DECODE(CODCON, 0, 'zero'), 
       NVL2(NUCHAVE, 'NÃO NULOOO', 'NULO')
FROM TMDFMG
WHERE ROWNUM < 10
ORDER BY DTENTRADA DESC